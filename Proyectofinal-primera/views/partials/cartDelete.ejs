<section class="m-5 border p-4">
    <form action="/api/carrito/<%= cartId %>?_method=DELETE" method="post" class="row g-3">
        <div class="col-md-12 text-center">
            <button type="submit" class="btn btn-primary">Eliminar Carrito</button>
        </div>
    </form>
</section>